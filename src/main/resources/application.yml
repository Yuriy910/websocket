spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/notification_db
    username: user
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

logging:
  level:
    root: INFO
    com.your.package: DEBUG